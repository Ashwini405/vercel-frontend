import { Request, Response, NextFunction } from 'express';
import { PoolConfig } from 'pg';
interface DBConfig extends PoolConfig {
}
export declare class IdealaneSDK {
    private jwtSecret;
    db: IdealaneDB;
    constructor(config?: {
        jwtSecret: string;
        dbConfig: DBConfig;
    });
    authMiddleware(): (req: Request, res: Response, next: NextFunction) => Response<any, Record<string, any>> | undefined;
}
export declare class IdealaneDB {
    private pool;
    private schema;
    constructor(configOrUrl: DBConfig | string);
    query(text: string, params?: any[]): Promise<any>;
}
export {};
